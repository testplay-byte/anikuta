package app.anikuta.extension.anime.api

import app.anikuta.extension.anime.model.AnimeExtension

/**
 * ANI-KUTA AnimeExtensionApi — minimal stub.
 *
 * TODO (later steps): copy the full implementation from aniyomi.
 * Fetches extension listings from extension repos.
 */
class AnimeExtensionApi {
    fun findExtensions(): List<AnimeExtension.Available> {
        // TODO: full implementation
        return emptyList()
    }
}
